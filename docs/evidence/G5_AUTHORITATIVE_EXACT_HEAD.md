# G5 authoritative exact-head verification commit

This administrator-authored commit follows removal of every one-time migration
and repair workflow. The branch now has no automation authorized to rewrite the
source tip. The ordinary `hepta-glasses-ci` run and the read-only
`g5-exact-head-closeout` round-trip must both bind this exact commit and tree.

Only E0-E4 source closure can result. Physical-device, deployed production,
repository-administrator, vendor-firmware, provider-operations, independent
assurance, signing and binary attestation, pilot, rollout, rollback, and store
approval remain real E5-E7 gates.

Canonical plan revision: `2026-08-31-g5`.
