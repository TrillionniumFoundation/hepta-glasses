name: demo_ai_even
description: "Hepta Glasses distributed AI-native edge runtime and Even G1 companion application."
publish_to: 'none'
version: 0.2.0+2

environment:
  sdk: '>=3.5.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.3+1
  get: ^4.6.6
  crclib: ^3.0.0
  fluttertoast: ^8.2.8
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
