name: g7-wave4

on:
  workflow_dispatch:
  push:
    branches:
      - codex/hepta-glasses-g7-synthesis
    paths:
      - .github/workflows/g7-wave4.yml
      - tools/apply_g7_wave4.py
      - tools/g7-wave4.trigger

permissions:
  contents: write

concurrency:
  group: g7-wave4-${{ github.ref }}
  cancel-in-progress: false

jobs:
  repair:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: codex/hepta-glasses-g7-synthesis
          fetch-depth: 0
          persist-credentials: true
      - run: |
          git config user.name 'hepta-g7-wave4[bot]'
          git config user.email 'hepta-g7-wave4[bot]@users.noreply.github.com'
      - run: python3 tools/apply_g7_wave4.py
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.47.2'
          channel: stable
          cache: false
      - name: Preflight conditional gate repair
        run: |
          flutter pub get
          dart format lib test
          python3 -m compileall -q services adapters tools
          python3 -m unittest discover -s services -p 'test_*.py'
          flutter analyze --no-fatal-infos
          flutter test
          git add -A
          git diff --cached --check
      - name: Commit repaired candidate
        run: |
          if git diff --cached --quiet; then
            echo 'No conditional-gate changes.'
            exit 0
          fi
          git commit -m 'fix(g7): make credential gate evidence-conditional'
          git push origin HEAD:codex/hepta-glasses-g7-synthesis
